name: Link Check

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]

jobs:
  linkcheck:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: latest
          extended: true

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install PostCSS CLI and autoprefixer
        run: npm install --no-save postcss postcss-cli autoprefixer

      - name: Build site
        run: hugo --minify --baseURL "https://docs.agopengps.com/"

      - name: Run Lychee (offline)
        uses: lycheeverse/lychee-action@v2
        with:
          args: >
            --offline --root-dir "${{ github.workspace }}/public"
            public/**/*.html
          fail: true
